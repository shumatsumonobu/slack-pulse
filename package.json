{
  "name": "slack-pulse",
  "private": true,
  "scripts": {
    "fetch": "node fetch-data.js",
    "dev": "npx serve .",
    "record": "node record.js"
  },
  "dependencies": {
    "@slack/web-api": "^7.0.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "playwright": "^1.58.2"
  }
}
